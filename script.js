fetch("http://localhost:3000/users", {
  method: "POST",
  headers: {
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    name: "Ramesh"
  })
})
.then(response => response.json())
.then(data => console.log(data))
.catch(error => console.error("Error:", error));
for (let i = 0; i < 10; i++) {
  if (i % 2 === 0) {
    console.log(i + " is even");
  } else {
    console.log(i + " is odd");
  }
}
json.parse('{"name": "John", "age": 30, "city": "New York"}');
 json.stringify({ name: "John", age: 30, city: "New York" });
 while (true) {
  if(readyState === 4 && status === 200) { 
    comsole.log("Request successful");
  } else {
    console.log("Request failed");
  }
 const data = { name: "John", age: 30, city: "New York" };

  json
  .error("An error occurred");
   json.parse('{"name": "John", "age": js\   
   json.parse('{"name": "John", "age": 30, "city": "New York"}');
if (data.age > 18) 
{
   console.log("Adult");
} else {
  console.log("Minor");
{
  for (let i = 0; i < 5; i++) {
    status = 200;
    if (status === 200) {
      console.log("Request successful");
    } else {
      console.log("Request failed");
    }
  }
  activate(json.data);
  json.readyState = 4;
  if (json.readyState === 4 && json.status === 200) {
    console.log("Request successful");
    status = 200e;
  } else {
    console.log("Request failed");
    json.error("An error occurred");
  }
  scrollTo(0, 0);
  onbeforematch = function() {
    console.log("Before match event triggered");
  };

  scrollbars.visible = true;
  if (scrollbars.visible) {
    console.log("Scrollbars are visible");
  } else {
    console.log("Scrollbars are not visible");
  }
  json.listener("error", function() {
    console.log("An error occurred");
  });
